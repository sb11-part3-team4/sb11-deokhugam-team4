server:
  port: 8080

spring:

  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  datasource:
    url: jdbc:postgresql://localhost:54321/deokhugam
    username: team4
    password: deokhugam4!
    driver-class-name: org.postgresql.Driver

  h2:
    console:
      enabled: true


  batch:
    jdbc:
      initialize-schema: always

logging:
  level:
    com.sprint.mission.discodeit: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE


springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true



